// DO NOT EDIT: This file is automatically generated by the project's build task'use strict';
var Esds = Esds || {};

// Polyfill closest() for IE 11 & Edge 14
if (!Element.prototype.matches)
    Element.prototype.matches = Element.prototype.msMatchesSelector || 
                                Element.prototype.webkitMatchesSelector;

if (!Element.prototype.closest)
    Element.prototype.closest = function(s) {
        var el = this;
        var ancestor = this;
        if (!document.documentElement.contains(el)) return null;
        do {
            if (ancestor.matches(s)) return ancestor;
            ancestor = ancestor.parentElement;
        } while (ancestor !== null); 
        return null;
    };

'use strict';
var Esds = Esds || {};

Esds.CodeSnippet = function() {
    function copyCodeToClipboard(e) {
        const trigger = e.target,
                snippet = trigger.closest('.eds-code-snippet');
        console.log(trigger, snippet);
                // source = snippet.querySelector('.esds-code-snippet__pre');
        // alert(source);
    }

    function getCopyTriggers() {
        return document.querySelectorAll('.esds-code-snippet__copy-code-wrap');
    }

    function enableCopyFunctionality() {
        const triggers = getCopyTriggers();
        triggers.forEach(function(t){
            console.log(t);
            t.addEventListener('click', copyCodeToClipboard);
        });
    }

    let init = function init() {
        console.log("SOMETHING");
        enableCopyFunctionality();
        // Find all copy triggers on the page
    };

    return {
        init: init
    };
}();
