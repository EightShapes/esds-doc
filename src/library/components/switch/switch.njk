{% macro switch(
	id=false,
	class=false,
	disabled=false,
	checked=false,
	text="Client facing view") %}

	{% if id == false %}
		{% set random_number = range(0, 100000) | random %}
		{% set id = label | replace(' ', '-') | lower + '--' + random_number %}
	{% endif %}

	<div class="{{ theme }}-switch{{' ' + class if class}}{{' ' + theme + '-switch--disabled' if disabled}}">
		<label class="{{ theme }}-switch__label" for="{{ id }}">
			<input id="{{ id }}" type="checkbox" role="switch" class="{{ theme }}-switch__input" {{ 'disabled' if disabled }}  {{ 'checked' if checked }}>
			<span class="{{ theme }}-switch__text">
				{{ text }}
			</span>
		</label>
	</div>
{% endmacro %}


{% macro switch_field_group(
            class=false, 
            horizontal=false) %}
    <div class="{{ theme }}-switch__field-group{{ ' ' + theme + '-switch__field-group--horizontal' if horizontal }}{{ ' ' + class  if class }}">
        {% if caller %}
            {{ caller() }}
        {% endif %}
    </div>
{% endmacro %}


{% macro switch_group(
	class=false, 
	label=false) %}
    <fieldset class="{{ theme }}-switch__group{{ ' ' + class  if class }}" role="group">
        {% if label %}
            <legend class="{{ theme }}-switch__group-label">
                {{ label | safe }}
            </legend>
        {% endif %}
        {%- if caller -%}
            {{ caller() }}
        {%- endif -%}
    </fieldset>
{% endmacro %}
